import { NoughtCrossPipe } from './nought-cross.pipe';

describe('NoughtCrossPipe', () => {
  it('create an instance', () => {
    const pipe = new NoughtCrossPipe();
    expect(pipe).toBeTruthy();
  });
});
