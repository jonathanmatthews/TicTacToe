import { TotalGamesPipe } from './total-games.pipe';

describe('TotalGamesPipe', () => {
  it('create an instance', () => {
    const pipe = new TotalGamesPipe();
    expect(pipe).toBeTruthy();
  });
});
